1. basicFunc
CallbackValue、LogicHanlerSettings、UIPanelBase
2. Logic
    身份验证
    Lobby管理：创建、加入/快速加入、查询、绑定LocalToRemote
    GameMgr：LocalLobby、LocalUser、EndGame(Host设置Locallobby.locallobbystate=LobbyState.lobby,locked=true,SendLocalLobbyData发送客户端刷新lobby,SetLobbyView(GameState=lobby,userState=lobby))
    SetupInGame：Relay创建填充/NGO(NetworkManager).StarHost、Relay加入填充/NGO.Join、OnGameEnd(NGUO.shutdown,Destory(networkObj.parent.gameObject),SetMenuVisible) -> GameMgr.EndGame
    InGameRunner：
	NetworkSpawn里连接确认、服务器生成连接player NGO，并检测全部玩家连接通知clients开始游戏。
	游戏结束：Outro动画，动画完成Host发送clients onGameEnd,然后Host自己再onGameEnd -> SetupInGame.OnGameEnd

3. UI
    Menu、JoinMenu(LobbyListUI)、Lobby(LobbyUseUI)

-----------------------
Todo:PopUI、GameManager